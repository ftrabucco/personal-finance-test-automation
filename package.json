{
  "name": "personal-finance-test-automation",
  "version": "0.1.0",
  "private": true,
  "description": "Test automation framework for Personal Finance.",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --grep @smoke --project=api --project=chromium",
    "test:contract": "playwright test --grep @contract --project=api",
    "test:staging:api": "TEST_ENV=staging playwright test --project=api",
    "test:staging:contract": "TEST_ENV=staging playwright test --grep @contract --project=api",
    "test:staging:destructive": "TEST_ENV=staging ALLOW_DESTRUCTIVE_TESTS=true playwright test --grep @destructive --project=api",
    "test:ui": "playwright test --grep @ui",
    "test:ui:serial": "E2E_WORKERS=1 playwright test --grep @ui",
    "test:ui:parallel": "E2E_WORKERS=4 playwright test --grep @ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report",
    "test:prod:smoke": "TEST_ENV=prod playwright test --grep @smoke-readonly --project=api --project=chromium",
    "install:browsers": "playwright install"
  },
  "keywords": [
    "playwright",
    "test-automation",
    "e2e",
    "api-testing"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.59.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
